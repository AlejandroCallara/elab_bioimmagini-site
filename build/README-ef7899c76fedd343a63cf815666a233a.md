# Elaborazione delle Bioimmagini

Materiale didattico per il corso di elaborazione di bioimmagini.

## Contatti

Per domande o problemi, contatta:
- Ing. Vincenzo Positano: positano@ftgm.it
- Dr. Alejandro Luis Callara: alejandro.callara@unipi.it


## Indice dei Contenuti

- [Prerequisiti](#prerequisiti)
- [Istruzioni di Installazione](#istruzioni-di-installazione)
  - [macOS](#macos)
  - [Linux](#linux)
  - [Windows](#windows)
- [Compilazione del Jupyter Book](#compilazione-del-jupyter-book)
- [Struttura del Progetto](#struttura-del-progetto)
- [Risoluzione dei Problemi](#risoluzione-dei-problemi)
  - [Problemi con la versione di Python](#problemi-con-la-versione-di-python)
  - [Errori durante l'installazione](#errori-durante-linstallazione)
  - [Problemi con l'attivazione dell'ambiente virtuale](#problemi-con-lattivazione-dellambiente-virtuale)


---

## Prerequisiti

**IMPORTANTE: Questo progetto richiede Python 3.12. Assicurati di avere Python 3.12 installato prima di procedere.**

---

## Istruzioni di Installazione

<details>
<summary><h3 id="macos">macOS</h3></summary>

### macOS

1. **Installa Python 3.12** (se non già installato):
   
   Usando Homebrew (consigliato):
   ```bash
   brew install python@3.12
   ```
   
   Oppure scarica da [python.org](https://www.python.org/downloads/) e seleziona Python 3.12.x

2. **Verifica la versione di Python**:
   ```bash
   python3.12 --version
   ```
   Dovrebbe mostrare: `Python 3.12.x`

3. **Naviga nella directory del progetto**:
   ```bash
   cd /path/to/elab_bioimmagini
   ```

4. **Crea un ambiente virtuale**:
   ```bash
   python3.12 -m venv .venv
   ```

5. **Attiva l'ambiente virtuale**:
   ```bash
   source .venv/bin/activate
   ```

6. **Aggiorna pip**:
   ```bash
   pip install --upgrade pip
   ```

7. **Installa le dipendenze**:
   ```bash
   pip install -r requirements.txt
   ```

</details>

<details>
<summary><h3 id="linux">Linux</h3></summary>

### Linux

1. **Installa Python 3.12** (se non già installato):
   
   Su Ubuntu/Debian:
   ```bash
   sudo apt update
   sudo apt install software-properties-common
   sudo add-apt-repository ppa:deadsnakes/ppa
   sudo apt update
   sudo apt install python3.12 python3.12-venv python3.12-dev
   ```
   
   Su Fedora/RHEL:
   ```bash
   sudo dnf install python3.12 python3.12-pip
   ```
   
   Oppure scarica da [python.org](https://www.python.org/downloads/) e compila dai sorgenti

2. **Verifica la versione di Python**:
   ```bash
   python3.12 --version
   ```
   Dovrebbe mostrare: `Python 3.12.x`

3. **Naviga nella directory del progetto**:
   ```bash
   cd /path/to/elab_bioimmagini
   ```

4. **Crea un ambiente virtuale**:
   ```bash
   python3.12 -m venv .venv
   ```

5. **Attiva l'ambiente virtuale**:
   ```bash
   source .venv/bin/activate
   ```

6. **Aggiorna pip**:
   ```bash
   pip install --upgrade pip
   ```

7. **Installa le dipendenze**:
   ```bash
   pip install -r requirements.txt
   ```

</details>

<details>
<summary><h3 id="windows">Windows</h3></summary>

### Windows

1. **Installa Python 3.12** (se non già installato):
   
   - Scarica Python 3.12.x da [python.org](https://www.python.org/downloads/)
   - Esegui l'installer
   - **Importante**: Seleziona "Add Python to PATH" durante l'installazione
   - Seleziona "Install for all users" se hai i diritti di amministratore

2. **Verifica la versione di Python**:
   
   Apri Command Prompt o PowerShell:
   ```cmd
   python --version
   ```
   Oppure se python3.12 è nel PATH:
   ```cmd
   python3.12 --version
   ```
   Dovrebbe mostrare: `Python 3.12.x`

3. **Naviga nella directory del progetto**:
   ```cmd
   cd C:\path\to\elab_bioimmagini
   ```

4. **Crea un ambiente virtuale**:
   ```cmd
   python -m venv .venv
   ```
   Oppure se usi python3.12 esplicitamente:
   ```cmd
   python3.12 -m venv .venv
   ```

5. **Attiva l'ambiente virtuale**:
   
   In Command Prompt:
   ```cmd
   .venv\Scripts\activate
   ```
   
   In PowerShell:
   ```powershell
   .venv\Scripts\Activate.ps1
   ```
   
   Se ottieni un errore di policy di esecuzione in PowerShell, esegui:
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

6. **Aggiorna pip**:
   ```cmd
   python -m pip install --upgrade pip
   ```

7. **Installa le dipendenze**:
   ```cmd
   pip install -r requirements.txt
   ```

</details>

---

## Compilazione del Jupyter Book

Dopo l'installazione delle dipendenze, puoi compilare il libro utilizzando il comando ufficiale di Jupyter Book:

```bash
jupyter book start

```


visita il sito [http://localhost:3000](http://localhost:3000) per visualizzare il libro.

